export default class BLEPeripheralServer {
	constructor(uniBLEPeripheralServer) {
		this.uniBLEPeripheralServer = uniBLEPeripheralServer
	}
	addService(object) {
		var service
		return this.addService(object)
	}
	close(object) {
		return this.close(object)
	}
	offCharacteristicReadRequest(callback) {}
	offCharacteristicWriteRequest(callback) {}
	onCharacteristicReadRequest(callback) {}
	onCharacteristicWriteRequest(callback) {}
	removeService(object) {}
	startAdvertising(object) {}
	stopAdvertising(object) {}
	writeCharacteristicValue(object) {}
}
